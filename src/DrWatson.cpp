#include "DrWatson.h"
using namespace std;

DrWatson::DrWatson(string short_name) : Hero(short_name, "DR.WATSON", 8, 2, "ranged") {}

void DrWatson::ability(Hero& hero, vector <Hero*> heros, CardManager& card)
{
    //No implementation
}