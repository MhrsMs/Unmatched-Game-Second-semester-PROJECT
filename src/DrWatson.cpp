#include "DrWatson.h"
using namespace std;

DrWatson::DrWatson(string photo) : Hero(photo, "DR.WATSON", 8, 2, "ranged") {}

void DrWatson::ability(Hero& hero, vector <Hero*> heros, CardManager& card)
{
    //No implementation
}